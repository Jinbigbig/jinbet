# results_history 镜像记录清理报告

日期：2026-09-14　｜　范围：`results_history/`（239 个日档 + index.json）

## 一、结论：镜像规模远超预期

原以为只是「亚冠类跨日主客镜像重复 **4 对**」，实测是两层问题：

| 层级 | 规模 | 性质 |
|---|---|---|
| 跨日孪生（同一 matchId 落在相邻两天、主客互换） | **1531 对** | 同日内重复 7 对，其余全是跨日 |
| 单条方向反转（不重复的单条记录主客颠倒） | **1244 条** | 历史抓取链路的系统性缺陷残留 |
| 合计冗余记录 | 4624 → 3059（删 1565、并 26） | — |

**根因**：早期 `fetch_163_results` 对每场「主客反序双写」，写 `日期_主_客` 与 `日期_客_主` 两条；
matchId 去重时按 dict 顺序取首个，于是「留正还是留反」随机 → 全库呈现 60.7% 反转的混合态。
该源头已于 2026-09-10 修复（`update_odds_net.py` 现按 matchId 唯一写档 + 方向对齐体彩 SCHEDULE），本次是清理历史遗留。

## 二、判据可靠性

方向锚 = 体彩官方赛果 API，按（matchDate ±2 天，无序队对）匹配（队名 canonical + 前缀兼容）。
内部一致性自检（**这是关键**）：

| 类别 | 数量 | 含义 |
|---|---|---|
| fwd-SAME（方向一致且比分相同） | 1722 | 正确，保留 |
| rev-MIRROR（方向相反且比分镜像） | 2670 | 确为翻转，需修正 |
| 自相矛盾（fwd-MIRROR / rev-SAME） | ~0 | 判据无误 |

即：所有判定与「比分是否镜像」100% 吻合，说明锚点没有错配。

## 三、修正效果

| 指标 | 修正前 | 修正后 |
|---|---|---|
| 主胜 / 平 / 客胜 | 35.0% / 26.4% / **38.6%** | **42.4%** / 26.4% / 31.3% |
| 场均主/客进球 | 1.38 / 1.46 | 1.57 / 1.28 |
| 与官方同向率 | 39.2% | **100%**（2941/2941，另 118 条无锚点原样保留） |
| 重复 matchId | 1538 组 | **0** |

修正前「客场占优」是异常信号，修正后恢复标准主场优势。

## 四、下游同步

- `league_profile.json` 重建：样本 4479→**3058**，基线 2.8417→**2.8506**，39 联赛（`score_mix.w=0.3` 保持不变）。
- `platt_params.json` 重拟合（原参数建于被污染的标签上）：n_samples 3424→**1981**，home/draw/away 三组参数均更新。

## 五、落盘

| 分支 | 提交 | 内容 |
|---|---|---|
| gh-pages | `0e318da` | results_history + index.json 重算 |
| master | `78a492d` | results_history + league_profile.json + `tools/prediction/dedup_results_history.py`(v2) |

备份：`_hist_backup_2026-09-14/`（清理前全量）；`league_profile.bak_20260914.json` / `platt_params.bak_20260914.json`。

## 六、未做与后续

- **未重发 09-14 已发布报告**：比赛日进行中，重算已发布预测等同事后编辑。新口径自下一交易日自然生效。
- 118 条无官方锚点记录保留原方向（114 单条 + 2 跨日 + 2 同日重复），已列明可复核。
- 历史复盘页（review_*）的累计命中率为旧数据所算，未回改；后续生成的复盘将用新数据。
