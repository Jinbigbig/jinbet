# 2026-09-16（周三）JinBet 每日流水线

## 场景
- 本地 gh-pages=319e789（09-15 收尾），远端真实 gh-pages=3eae9a0（含 2 个 CI 数据提交 dcd18ac / 3eae9a0），本地是其祖先 → `merge --ff-only origin/gh-pages` 快进，无分叉、无需 rebase。远端无 09-16 报告（外部作业今日未生成）。根 index.html 无 `data-page-node-id` → 未 checkout。
- 本地 master 分支陈旧（2ea3515，落后 9 提交）→ 一律用 `git show origin/master:...` 取正本、worktree 以 ls-remote 真实 SHA 为基点；收尾时 `git fetch origin master:master` 已把本地 master 快进到 c11f99b（陈旧问题本次消除）。

## 步骤0 与赛果库第三次复发（自愈已生效，无需人工）
- 快进后 results_history 落到**污染态**：4602 条 / 1531 组重复 matchId / 主客 34.9-26.3-38.7（镜像孪生又被 CI 数据作业写回）。
- 步骤0 两遍（先 `--no-push` 赔率路径 → 再 `--no-push --results-only`）后 `[DEDUP-ALIGN]` 自动跑 `dedup_results_history.py --apply`：**3086 条 / 0 重复 / 主客 42.6-26.3-31.2**（健康）。受影响文件 235/241，脚本自备份到 `_hist_backup_2026-09-16`。
- 结论：**归档污染是每次步骤0 都会复发、每次都会被自愈的常态**，不必惊慌也不必手工 dedup；但如果哪天日志里没出现 `[DEDUP-ALIGN]`，说明 `dedup_results_history.py` 找不到 → 它会 warn 跳过，此时才需处理。
- 步骤0 odds 路径本次「未发现空赔率别名行」「未发现重复比赛」，SCHEDULE 无别名双行。

## 数据与引擎
- 17 场（欧罗巴 x6 / 西甲 x4 / 亚运男足 x2 / 解放者杯 x2 / 亚冠精英 x1 / 英联赛杯 x1 / 巴甲 x1），1X2 赔率 17/17（003/008/013 三场由比分盘反推）。
- Platt 样本 2005 场；联赛档案 3058 场 / 39 联赛 / 基线 2.8506 / score_mix.w=0.3；二级盘校准 n=4162。
- V3 全模块 ON（BRIER_OPT / KALMAN ON·生效，HEDGE ON·观察）。DRIFT_MONITOR 仍报 league_baselines 漂移（德乙 50% / 韩职 16.7% / 荷甲 15.4%），与 09-15 相同，未处理。
- 星级分布：5★×4（003/006/008/013）、4★×1（009）、3★×1（002）、2★×4、1★×7。

## 当日改动（已同步 master c11f99b）
1. `league_match.ALIAS` 补齐**巴甲 20 队**（博塔弗戈→保地花高、格雷米奥→甘美奧、帕梅拉斯→彭美拉斯、巴竞技→帕拉尼恩斯、米竞技→明尼路、布拉干RB→巴拉干天奴、里莫→瑞模貝雷PA 等），逐条按 7M 巴甲积分榜队名核对；排名反填 4/17 → 5/17。
2. `league_match.UNAVAILABLE` 增 `欧罗巴`/`欧协联`（原只有 `欧联`），报告第七节对今日 6 场欧罗巴从「暂无数据源」变为明确「杯赛赛制无联赛积分榜」。
3. `gen_report.py` 星级分布串漏 `1★` 档（只列 5/4/3/2★），17 场只显示 10 场 → 补 `1`；修后「4场5★ / 1场4★ / 1场3★ / 4场2★ / 7场1★」。

## 复盘 09-15（三指标）
- 方向 9/14=64% ✅ / 单点 0/14=0% ❌ / 双档 0/14=0% ❌ / **λ 偏差 −1.54 球/场** ⚠️（阈值 0.2 的 7 倍）。
- 已核到根因：09-15 是真实高进球日（14 场共 58 球、场均 4.14），单场 4:6（叻武里vs上海海港，半场 0:3，winFlag=A，双字段自洽）等极端比分把偏差拉爆；预测均值 2.60。方向命中 64% 正常 → 非模型故障，不触发重拟合。
- 累计（09-08~09-15，126 场）：方向 61.1% / 单点 11.9% / 双档 19.8%（单点、双档均低于基准 14.9% / 26.5%，但 126 场样本下约 −0.95σ，继续观察）。

## 落盘
- gh-pages `8714c8b`（3eae9a0..8714c8b 快进）｜master `c11f99b`（817f333..c11f99b，worktree + `push HEAD:master`）；ls-remote 双核验 ✅；线上报告 / 复盘 / 索引三页均 200。
- 未提交：`predictions/2026-09-15/*.png`（09-15 17:09 重生成、晚于 319e789 提交时间，来源不明，保持工作区脏状态不并入）。

## 下个交易日注意
- 报告「高信心场次」文案的星级分布现已含 1★，核对时别再以为漏档。
- 巴甲排名已可自动填；新增其他联赛队名仍须先补 `ALIAS`。
- 欧罗巴/欧协联走 UNAVAILABLE 分支，报「杯赛赛制」而非「获取失败」，属预期。

## 晚间：DRIFT_MONITOR 漂移排查 + 重拟合前后对照（用户"跑一下对比看看"）
- **结论1：漂移是数据清理的副产品，非真实赛季偏移。** `drift_baseline.json` 基准快照取自 09-14（清理前污染数据），09-16 第三步自愈合把 results_history 从 4602→3086（主客方向修正）后，联赛均值整体平移 → 监控报 `league_baselines` 标准差 7.19%（≥5% 阈值），top3=德乙50%/韩职16.7%/荷甲15.4%。**删除基准重跑 `drift_check` 重建（用干净 3085 场）→ 标准差 0.0%、全部稳定**。三个联赛回测准确率前后完全不变（德乙38.8%/韩职43.4%/荷甲52.9%）→ 证实漂移是假阳性。
- **结论2：Platt 权重口径修复有效但幅度小。** 改 `fit_platt_params` 固定 `MARKET_W=0.80`→动态 `league_market_w(league)`，重拟合后 Platt 参数变化明显（平局 a 0.3524→0.4359），对应平局行命中 43.9%→44.2%。全窗口 before/after：
  - 1X2 准确率 51.10%→51.26%（+0.16pp；vs 市场差 −0.73→−0.57pp）
  - Brier 0.19857→0.19850（−0.00007）；LogLoss 1.00429→1.00371（−0.00058）
  - 模型独有命中 202→214、两者都错 984→972、平局行命中改善。三项一致正向，2462 场下约 +4 场正确、统计边际显著但非质变。
- **操作**：已本地落地（非 git 提交，待用户确认）：①`_calc_engine.py` 加 MARKET_W→`league_market_w` 修复；②`build_league_profile.py` 重建（3085 场/基线2.854）；③`platt_params.json` 强制重拟合（fitted_at=09-16）；④`drift_baseline.json` 重建基准。备份在 `_calib_backup_20260916/`（含 before/after 回测 JSON 与原校准文件，可一键回滚）。临时隔离目录 `_bt_run/` 用后即删。
- **落库建议**：低风险的净改善 + 真 bug 修复，建议应用。注意 `league_profile.json`/`platt_params.json` 当前是**未跟踪**本地工件（不随 gh-pages 共享），`_calc_engine.py` 修复须走 master `tools/prediction/calc_engine.py` worktree 同步；`drift_baseline.json` 已跟踪会随 commit 生效。待用户确认是否 commit+push。

## 晚间：V2.2 留档 vs V3.4 快照对照（用户给 Marvis 导出的 pred_table_2026-09-16.html）
- 用户指出该 HTML（引擎标 `poisson-v2.2`，数据源写「网易体彩 163」）是用 2.2 跑的。将其 17 场与今日 V3.4 `predictions/2026-09-16/pred_snapshot.json` 逐场比对：
  - **胜平负方向 17/17 完全一致**；**主推比分 17/17 完全一致**；**星级 17/17 完全一致**（5★×4/4★×1/3★×1/2★×4/1★×7）；**三档比分(集合+顺序) 17/17 完全一致**。
- **关键发现**：V3.4 快照的 `engine` 字段**本身也写着 `poisson-v2.2`**（版本字符串从未随 V3.x 模块升级而改）→ 这份 Marvis HTML 与今日流水线产出是**同一引擎的同一份预测**（连三档比分顺序都一致，不可能是两个独立模型巧合）。所谓「2.2 vs 3.4」对这一天而言在头条预测上**无差异**；V3.4 的增量只在校准/监控（即上面 +0.16pp 那次），不改变选哪队/选哪个方向/选哪个比分。
- **待办/flag**：引擎版本字符串 `poisson-v2.2` 已过期，应随 V3.4 改版（否则日后「2.2 vs 3.4」对照永远分不清）。Marvis HTML 数据源标「163」而流水线用 `index.html` SCHEDULE——该 HTML 大概率就是 jinbet 输出的再渲染版，或同源克隆。

## 晚间：比分双档口径改为「第 2、第 3 可能比分」（用户指令）
- 旧双档 = 稳档(联合众数 top_scores[0]) + 胆档(λ 期望取整)，互补非同质，文案「长期约 26%」。
- 新双档 = `top_scores[1]` + `top_scores[2]`（模型排序第 2、第 3 可能比分），语义统一为「头条若没中，最该补的两档」。
- 改动文件：`_gen_report.py` 的 `hit_band()`（重写取值逻辑 + 3 处展示文案「稳档押中+胆档量级→第2/第3可能比分」「约26%→约20%」）；`gen_review.py` 的 `predict_scores()` 把 `band=ts[:2]` 改为 `ts[1:3]`（报告与复盘口径一致），同步更新 docstring/基准说明（Top2 11.6%+Top3 8.9%≈20.5%）。
- 验证：`hit_band(m,2)` 抽样 9 场全部对齐 top_scores[1:3] ✅。重渲 `predictions/2026-09-16/index.html` + 重跑 `gen_review --date 2026-09-15`：复盘双档命中 0/14→**2/14=14%**（新口径多中 2 场）。已推 gh-pages，脚本经 worktree 同步 master `tools/prediction/`。
