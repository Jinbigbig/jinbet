# 2026-09-13 工作日志

## JinBet 每日预测流水线（14:00 自动化）
- 09-13 报告已生成并推送：gh-pages `0f2a2a0`，线上 200。
- 处理外部作业旧链路提交：reset --hard origin/gh-pages 后重跑全链。
- 修复 RESULT_TEAM_NAME_MAP：补 `埃尔沃斯堡→埃沃斯堡`、`拜仁慕尼黑→拜仁`，解决周日014 别名双行；已同步 master（b54cbc1，worktree 方式）。
- 数据：24 场，1X2 19/24；H2H≥3 场 1/24（皇家社会vs马竞 f=1.3）。
- 复盘 09-12：方向 47% / 双档 17% / λ偏差 -0.47（连续两日 >0.2，方向跌破 50% → 需关注重拟合）。
- 串关信心A：塞尔塔1-0×弗拉门戈2-0×那不勒斯1-0（241.80）。
- 经验：gen_report 不更新 predictions/index.html，索引条目需按 matches_data.json 手工插入 reports 数组。

## 追加（14:5x 用户反馈「比分连续两天偏差过大」）—— 专项回测 + 展示层修复
- 新增 `score_bias_audit.py`：9 天 135 场快照 vs 赛果逐日对账 → λ 偏差 −0.17 球/场（09-11 −0.63、09-12 −0.47）；头条比分命中仅 7%（小样本），top1 众数 13%。
- 新增 `lambda_level_probe.py`（1591 场，2026-01~09）：**λ 无系统性低估** —— 1~8 月模型高估 +0.12，9 月实际场均升至 3.05~3.13（高进球期）才导致近期负偏差。k 扫描 k>=1.10 使 MAE/LogLoss/Brier 全线变差 → 不追高。
- 比分规则对照：象限众数（现状）15.4% 单比分 / 66.4% ±1球 / Top3 40.3% / Top5 56.3% / 方向 53.8%；全局众数 16.6% 但 60% 退化 1:1（弃）；方向+总进球区间 27.4%。
- 串关口径对照：方向串关 3 腿全中 **25.3%**（ROI −3.0%）vs 比分串关 **0.5%**（198 天 1 天）。
- 展示层改动（`_gen_report.py`，已推 gh-pages `e291ffc` / master `3f1657f`）：①新增「方向串关」表作主口径（3 组，27.8%/15.9%/24.7% 概率）；②比分串关改名「高赔彩票型」并标注真实命中率 <1%；③4.1 表新增「±1球覆盖」列；④新增「总进球偏差归因」汇总卡。
- 引擎概率层未改（λ 未调）；校准窗口 7→14 天作为可选优化未部署。
- ⚠️ 发现仓库有另一会话在活动：本地 origin/* 跟踪引用被覆写、本地 update-ref 不生效、出现重建的 master 历史（3b855cd 系）。已按 ls-remote 核验远端 master=3f1657f、gh-pages=e291ffc 均正确。

## 追加（15:0x 用户再反馈「不只是串关，单比分预测和实际赛果出入很大」）—— 单比分专项诊断
- 新增 `score_rule_probe.py`（1591 场生产口径重建）：把「单比分错」拆成三层 —— **方向 53.8% × 方向正确下比分 28.6% = 15.4%**；总进球众数命中仅 28.0%。
- **关键结论：模型没算偏。** 逐格校准误差 ±1~2pp（1:1 模型11.8% vs 实际13.2%、1:2 5.6% vs 7.7%、2:0 7.2% vs 6.2%）；P(≥3) 模型52.2% vs 实际53.4%；模型自认众数 13.73% vs 实际命中 15.4%、自评±1球 60.4% vs 实测 66.4% —— **两项都偏保守**。
- **「出入很大」的真实来源＝单点众数必然低于均值**：头条比分总进球系统性比实际 **低 0.90 球/场**（主 −0.35/客 −0.55）→ 高频「预测1-0、实际3-1」。
- 9 种头条选法对照：现状象限列出众数精确率仍最高 15.4%；全局众数 16.6% 但 60% 退化 1:1（弃）；**round(λ) 13.8%（±1球 71.0%、总进球偏差 −0.22）**；象限+总进球众数 11.4%；λ最接近 10.8%；3x3平滑/最大±1覆盖 11.6%（±1球 71.4%、偏差 +0.07）。
- **温度展平测试（g∝g^(1/T)，T=1.0~1.35，前60%拟合/后40%时间外）**：测试集 LogLoss 单调变差 2.7635→2.8254 → 比分矩阵**不过尖**，引擎不动。
- 分档：λ≤2.3 → 单比分 23.8%（±1球 75.3%）；2.3~3.0 → 14.9%；>3.0 → 13.0%。
- ⚠️ **踩坑（重要）**：算 ±1 球邻域**不能用 `sum(g[max(0,h+dh)])`** —— h0/a0 为 0/1 时 max(0,·) 会把同一格重复计数，band 被虚高到 123%~386%（一度误判「模型过自信 15pp」，修正后实际是欠自信 6pp）。必须用去重格集合。
- 展示层改动（`_gen_report.py`，gh-pages `e792a90` / master `a05f7c2`）：①卡片新增「比分双档」（象限内前两档合计，回测 25.5%）；②新增「期望比分」= λ 四舍五入（象限内修正），标注总进球偏差 −0.90→−0.22、±1球 66.4%→71.0%；③新增「比分可信度」分档标签（低λ23.8%/中14.9%/高λ13.0%）；④4.1 表加「期望比分」「比分可信度」列，方向首选单元格内嵌双档合计；⑤卡片脚注与汇总区新增「单比分误差归因」全文。
- 今日 24 场中「期望比分」与「概率首选」不同者 9 场（如 003 塞尔塔 首选1-0/期望2-1、010 莱万特vs巴萨 首选1-2/期望1-3）。

## 追加（15:1x 用户定调「众数必然低于均值，这种方式我不需要」）—— 头条口径切换为期望值
- **决策：报告头条比分弃用概率众数，改为「期望比分」= λ 期望进球四舍五入（并约束在倾向象限内）**。众数只保留内部用途（判倾向、约束象限、兜底），`direction_pick` docstring 已注明。
- 新增 `_exp_band_probe.py`（1591 场，已同步 master `tools/prediction/exp_band_probe.py`），实测三项：
  - 双档口径对照：象限前两档 25.5% → **期望比分+象限次高 26.0%**（同为 2 档且含头条，故改用后者）；期望比分+象限前两档 27.8% 但平均 2.21 档，未采用。
  - 期望比分口径 λ 分档：λ≤2.0 → 单点 **25.6%** / ±1球 85.4%；λ>2.0 后各档稳定在 7~18%（**不再随 λ 单调下降**）。
  - ⚠️ 旧「低λ 23.8% → 高λ 13.0%」是**象限众数口径的性质**，改口径后不成立 → 「比分可信度」分档已按新口径重测（≤2.0 较可信 / >2.0 仅参考或不可照抄，语义改为「能不能照抄」）。
- 改动清单（gh-pages `5521652`+`f890796` / master `08a3813`+`0fe53f2`）：
  1. 卡片：期望比分升为第一行头条（带 λ 与模型给该比分的概率）；删除「方向首选」众数行；±1球覆盖改以期望比分为中心；新增「比分口径说明」。
  2. 双档：`expect_band()` = 期望比分 + 同倾向次高（保证与头条一致，不再出现「期望2-1 而双档1-0+2-0」）。
  3. 4.1 表：列改为 期望比分 / 比分双档 / ±1球覆盖 / Top3 / Top5 / 可信度；删除「方向首选」「次选/三选」两列。
  4. 比分串关（信心/冷门）腿改用期望比分并标注真实概率（A 组仅 0.11%）。
  5. 联赛形势段「首选 sc」→「期望比分」。
  6. `gen_review.py` 复盘口径与报告对齐：新增「期望比分单点命中」，双档改为含头条口径（弃全局 top2）。
- 09-12 复盘（新口径）：方向 14/30=47% | 期望比分单点 2/30=7% | 双档 4/30=13% | λ偏差 −0.47。
- 线上：报告页与复盘页均 200；`方向首选` 字样已从报告清零。



- 【用户质疑「保留众数不准，概率高有什么意义」→ 全量复核，结论反转】
  1. 现状星级（比分矩阵众数概率）实测**并非无信息量**：5★ 方向 65.1% / 2★ 45.4%（跨度 +13.6pp），我此前「星级无信息量」的判断有误。
  2. 但「平均 λ 总量」随星级从 5★ 2.06 单调升到 1★ 5.04 → 高分档大半是「低进球场」的代理。
  3. 分层检验（控制 λ）：同 λ 区间内「概率高半」方向命中仍高 +10~24pp → 非纯代理。
  4. **判别力对比（决定性问题）**：五等分档方向命中跨度 —— 比分众数概率 +13.6%、期望比分概率 **−1.7%（完全无效）**、**胜平负倾向概率 +36.6%（完美单调，38.1→74.6%）**。
  5. 落地：引擎星级改用 Platt 后 max(1X2)，阈值 0.44/0.50/0.57/0.66；4.4 节与卡片「信心评级」行同步（新增「方向确定性 xx%」）。旧口径两个数据（+13.6pp / 旧倒U结论）已作废。
- 新工具 `star_value_probe.py`（A 星级分档实测 / B 候选概率对照 / C 单调性 / D 控制λ分层 / E 三种概率判别力对比 / F 新星级逐档+冷门率）。
- 今日新星级分布：5★×6、4★×9、3★×2、2★×3、1★×4（曼联vs曼城、赫塔费vs拉科等低确定性场次落到 1★，符合直觉）。
- 线上：报告页 200，含「方向确定性」24 处、「37.5%→74.4%」24 处，旧文案 0 残留。gh-pages `d892fbe`｜master `82ec2fb`。

- 【用户定调「主要优化方向=比分准确度，概率大没有意义，命中次数多了函数才好优化」→ 目标函数显式化 + 口径回正】
  1. **数学前提（已自证，H1）**：给定比分分布 g，使「押中」概率最大的选择**唯一**=argmax(g)；
     任何单调变换（温度展平 T=1.35 / 锐化 T=0.85）都不改变 argmax → 四种 T 检验集命中完全相同（117/637）。
     ⇒ **概率幅值与命中率无因果关系，只有排序对不对有关**。这直接回答了「概率高有什么意义」。
  2. **口径对照（H2，1591 场，后 40% 检验集 637 场）**：联合众数 **18.4%** ＞ 象限列出众数 16.3% ＞
     市场比分盘去水众数 17.1% ＞ 模型×市场混合 17.3~17.9% ＞ DirSwap（方向取市场）15.5% ＞ **round(λ) 13.7%（上一版部署，最差）**。
     全样本：16.6% / 15.4% / 14.3% / 16.0~16.6% / 15.5% / 13.8%。
     **McNemar 配对：联合众数 vs round(λ) χ²=4.79（p<0.05 显著，约 45 场/1591）**；vs 象限众数 χ²=1.16（不显著）；vs 市场比分盘 χ²=6.38。
     ⇒ 上一版「期望比分」头条在「命中次数」目标下是错的，已回正。
  3. **穷举后仍无排序源能赢众数**（全部±0.4pp 噪声内）：格子级乘性纠偏 B=(实测频率/模型概率)^γ 最优 18.8%、
     分区(方向×λ4档)条件经验重排最优 18.8%、总进球边缘往市场总进球盘纠偏最优 18.7%、Dixon-Coles τ 17.3~18.5%（ρ>0 更差）、
     市场比分盘混合最优 17.9%。4 折时间稳定性：联合众数 16.6% 在 4 折中 3 折第一。
  4. **提升空间只在「分布」不在「选法」**：单比分 = 方向 53.8% × 方向内 ~31%。
     λ 水平已最优（k=1.10/1.15 全线变差，勿追高）；要再提命中只能靠更准的方向/形状或新信息源（CLV 赔率漂移、阵容伤停）。
  5. **λ 分层实测命中率（H5，新「比分可信度」依据）**：λ≤2.3 → Top1 27.3%/Top2 43.6%/Top3 56.8%；
     2.3–3.0 → 16.0/30.1/39.4；>3.0 → 12.6/24.0/34.0。
  6. **双档/Top3 定义（H10）**：不约束象限的众数前 k 档 16.6%/**29.9%**/**39.9%**；
     旧「象限内前两档」25.5%/32.2%、上一版「期望比分行+同倾向次高」26.0%（双档少中 61 场）。
     ⇒ 象限约束会排掉跨象限次优格（如主场场次里的 1:1），三项全低。
  7. **注意**：联合众数与发布倾向**不同象限占 61.4%**（P(主胜)>P(平) 但 P(1:1)>P(1:0) 属正常），
     该子集里发布方向对 46.3% / 众数方向对 30.2%，但众数仍赢在**单格**概率 —— 报告卡片已加提示文案说明这不是自相矛盾。
  8. 落地：`_gen_report.py` 头条 → `hit_pick()`（联合众数）、双档 → `hit_band()`、`score_cred()` 改按新分层、
     `expect_score()` 降级为「量级参考」；串关腿改用联合众数；文案 10 处重写。
     `gen_review.py` 同口径 + 新增 `--kpi` **累计目标函数**（每页复盘页脚注入累计命中次数，作为后续优化的度量基准）。
  9. 已推：gh-pages `56f1933`（09-13 报告 + review_2026-09-12 重算）｜master `cc86f00`（gen_report/gen_review/score_hit_probe）。
     线上报告页与复盘页均 200。新探针 `score_hit_probe.py`（H1 前提自证/H2 排序源/H3 纠偏/H4 纠偏×市场/H5 分层/H6 总进球纠偏/H7 4折+McNemar/H8 条件重排/H9 DC/H10 双档定义）。
  10. 累计 KPI（09-08~09-12，76 场，新口径）：方向 45（59.2%）· 命中比分单点 10（13.2%）· 双档 18（23.7%）；
      基准线为 1591 场回测的 16.6%/29.9%（小样本，±4pp 内属噪声）。09-05~09-07 快照无 top_scores、且 09-10 前赛果主客颠倒，故 KPI 起点定 09-08。

- 【用户追加「比分预测不是分布式算的，是根据因素得出来的，众数和概率没意义」→ 逐条验证 + 三项落地 + 一个真缺陷】
  1. **验证①（用户对）**：λ 是 4 组因素的确定性函数 —— 近期战绩EWMA → 主客场分拆收缩 → 联赛先验收缩 → 市场概率混合；
     纯泊松下 ⌊λ⌋ 就等于联合众数 ⇒「众数」不是另一套算法，是**同一答案的另一种写法**。`_score_from_factors.py`。
  2. **验证②（用户对）**：单调变换（温度/锐化）不改变 argmax ⇒ 概率**数值**与命中率无因果。
  3. **验证③（用户不对）**：4036 场（2026-01-01~09-12，生产口径 w=0.3）——
     联合众数单点 **14.94%** ＞ floor(λ) 13.85% ＞ round(λ) 13.06% ＞ **无脑全猜 1:1 常数基线 12.93%**；
     对数基线 **净多中 81 场、McNemar χ²=19.34（p<0.001）**；对 floor/round χ²=6.47/8.11 亦显著。
     ⇒ 比分层**有**净信息量。完整阶梯：Top1 14.94 → Top2 26.54 → Top3 35.48 → Top4 43.93 → Top5 51.78 → ±1球 67.24%。
  4. **真正的病灶是「退化」不是「众数」**：65.8% 场次首选落在 1:1（独立泊松联合众数=(⌊λ主⌋,⌊λ客⌋)，
     双方 λ 都落 [1,2) 时必然 1:1），加上「全猜 1:1 就有 12.93%」⇒ **比分必须挑场次看**。
     **新增报告开头「🎯 今日比分精选」**（`_daily_top_probe.py`）：按模型自评概率排序每日取前 k 场，
     单比分命中 前1场 21.16% / 前2场 **21.88%** / 前3场 20.50% / 前5场 19.64%（对照全场次 14.94%），
     取前3场时 **49.0% 的比赛日至少中 1 场**；模型自评概率五等分档实测命中**单调** 10.8%→20.0%。
     等价排序器（噪声内）：λ总量最低 20.78%、概率÷λ总量 20.78%、P(1:1)最高 19.53%。
  5. **⛔ 调因素被实证否决**：`factor_hit_probe.py` F3/F4 —— 坐标上升在训练集（2421场）挑出 8 项改动、多中 28 场，
     检验集（1615场）**反少中 21 场**，λMAE 1.8036→1.8805、Brier 0.5953→0.6634 全线变差（纯过拟合）。
     F1 因素链贡献量级：市场概率混合 |Δλ|/场 0.555（唯一大旋钮）＞ 主客场分拆 0.220 ＞ 联赛收缩 0.141。
  6. **🔧 查清一个长期配置不一致**：`league_profile.json` 的 `score_mix.w` 自 w=0.3 时代起**一直是 0.3（线上真值）**，
     但引擎注释（第166/378行）、`factor_hit_probe` 的 P0、项目记忆都按 **0.5** 处理 —— 文档口径≠线上口径，
     且这种错位从探针输出里看不见（探针会把 0.5 当作"生产口径"）。**已让探针改为从档案读值**（`sync_from_profile`）。
     是否升 0.5（`_mixw_probe.py`）：全样本 0.3→0.5 命中 609→630（+21场），但时间外 0.3=272 / 0.5=270 / 0.6=275
     （噪声地板 16 场）**不显著** ⇒ **维持 0.3 不动**（改动会连带触发 Platt 重拟合，收益不可证）。
     ⚠️ 另：`_calc_engine.py` 第1433行注释本来就写 0.3（是对的），错的是另外几处。
  7. **🔧 新增：1X2 缺失场次用「比分盘」反推**（`_score2hda_probe.py` 验证 + `_local_prepare.py` 落地）。
     竞彩 ODDS 天天有若干场 1X2 为空但比分盘 31 档完整（09-13 有 **5/24**：埃沃斯堡vs拜仁、莱万特vs巴萨、
     本菲卡、埃因霍温、哈马比），这些场次原本降级为「纯模型 λ」——而结论是 纯市场 > 生产@0.80 > 纯模型。
     反推 = 31 档比分赔率去水后按主/平/客三区聚合。**验证**：在赔率齐全场次上对照，
     **2026-09-03 起的数据 方向一致率 100%、平均偏差 1.0~2.2pp、约 80~90% 场次 ≤2pp**；
     09-01/02 及更早为 **0%**（归档比分盘主客被翻转的脏数据）⇒ **反推只用于当日新鲜比分盘，不做历史回填**。
     落地后赔率完整度 **19/24 → 24/24**，报告卡片对这 5 场加「1X2 由比分盘反推」标注。
  8. **🔧 因素链补齐**：`chain` 此前缺「比分形状混合 / Platt / 比分矩阵对齐」三步 ——
     这既是「比分怎么推出来」缺的最后一环，也是 w 口径不符长期没被发现的原因。已补入 chain 与报告因素链表（①~⑩）。
  9. **展示层**：`_gen_report.py` 新增 `picks_sec`（比分精选）、`score_cred` 改按 4036 场重测分档
     （λ≤2.6 → 单点 18~22%；>2.6 → 11~13%；阈值 2.6/3.5），头条/双档/覆盖/4.1 表数字全部换到 4036 场口径。
  10. 已推：gh-pages `c5a6875`（09-13 报告 + review_2026-09-12）｜master `fb99850`（7 个新探针 + 6 个改动脚本 + profile）。
      线上报告页与复盘页 200。数据已按步骤0刷新（远端有 CI 16:18 数据提交，先 `reset --hard 0c97d5c` 再重跑全链）。
  11. 新探针：`score_from_factors`（因素直推 vs 取整口径）、`baseline_probe`（常数基线/分层）、
      `pick_mcnemar`（择格配对检验）、`daily_top_probe`（每日精选）、`mixw_probe`（形状混合权重）、
      `score2hda_probe`（比分盘反推1X2）、`factor_hit_probe`（因素消融/坐标上升）。

## 报告「去工程化」（用户指出报告里出现内部分析卡片）
- 用户截图质问：报告第五章出现「📈 总进球偏差归因 / 🎯 比分口径一致性 / 🎯 比分目标函数与口径」三张卡片。根因＝近日把**开发日志/回测口径/方法论**当作 summary-card 写进了公开报告页（`_gen_report.py` strategy_sec 的 summary-grid），且逐场卡片里还塞了含 `score_hit_probe.py`、`McNemar χ²`、`argmax`、`league_profile.json` 的长注释（×24 次重复）。其中一张还原文引用了用户的私下指示，属公开页泄私 + 内容腐化双重问题。
- **处理原则（新增项目约定）**：报告正文只保留「今日可执行结论」；方法论、回测口径、脚本名、统计量（χ²/Brier/MAE/LogLoss）、内部配置名一律不进报告，只留在 memory 与 `tools/prediction/` 探针里。
- 改动：①删 3 张方法论卡片；②24 张逐场卡片的口径长注释全部压成一句话；③删除 4.1/4.3/4.4/4.5 表注里的回测细节与公式（`logit(P)=a+b1·…`、`P=σ(β·x)`）；④比分精选节与串关注去统计化；⑤去掉 `(V3.3)/(V2.2)/(2026-09-10 新增)/(2026-09-13 定调)` 等版本/日期戳；⑥`gen_review.py` 累计 KPI 行去掉「目标函数/口径基准」措辞。
- 效果：`predictions/2026-09-13/index.html` **436KB → 296KB**（−32%）；线上已核实 7 个关键词全部清除。
- 已推：gh-pages `3d318bc`｜master `3c60569`（gen_report 79KB→68KB + gen_review）。远端随后叠加了 CI 数据提交 `9c4362e`（只改根 index.html/odds_data.json/version.txt，未触碰 predictions/）。
- ⚠️ 教训：以后往报告加内容前先问「这是给用户看结论，还是我自己的开发记录？」——后者写 memory / 探针，不写报告。

## 报告「去对话化」（用户第二轮截图：仍有对话式表述）
- 用户第二轮截图指出：上一轮只清了第五章方法论卡片，但报告里仍有「为什么单列这一节」「本节不改变任何预测」「无脑全场猜 1:1 就有 13%」等解释性文字，且把私下对话原文贴进公开页的问题没清干净；master 副本的探针注释里还有「用户定调/用户论点/你说…」。
- 本轮清理位置：①🎯 今日比分精选节「为什么单列这一节」整段 → 中性结论；②逐场卡片「无脑全猜 1:1 就有约 13%」「本节不改变任何预测」→ 删；③4.5 大比分节「反直觉但要记住」→ 中性；④「诚实概率」「该押哪几场」等措辞 → 中性；⑤`factor_hit_probe.py`/`_score_from_factors.py`/`big_goals_probe.py`/`market_edge_probe.py` 注释里的「用户定调/用户论点/你说/得出来的」→ 中性工程表述（`_neutralize_user_refs.py` 辅助批量替换）。
- **自查命令固化**：`grep -E "用户|你说|定调|目标函数|口径一致性|偏差归因|probe|χ²|Brier" predictions/<date>/index.html` 必须为**空**（不再只查脚本名，要把第一二人称/对话词也纳入）。
- 已推：gh-pages `f737f8e`｜master `fe57086`（脚本注释去个人化）。两者均 fast-forward（gh-pages 基于 CI `9c4362e`、master 基于 CI `2db9bb6`）。
- **线上验证**：`curl` 抓取 index.html 296,176 字符，20+ 关键词（用户/你说/为什么单列/本节不改变/无脑全猜/反直觉/诚实概率/该押哪几场/定调/口径一致性/偏差归因/目标函数/probe/χ²/McNemar/argmax/Brier/4036/1591/4217 …）**全部清除**；复盘页 9,752 字符同样清除。GitHub Pages 构建约 90s 后生效。
- ⚠️ 公开页三不落 = **不落方法论、不落对话、不落统计量**；脚本注释同样要去个人化（master 副本探针注释一并清理，已 amend 进 fe57086）。

- 报告改版（用户反馈）：4.1 胜负与比分预测 去竖排——命中比分/双档单元格改单行（比分+概率），删 λ 子行；冷门列改为直接显示冷门概率（带风险色）；说明段精简去掉「量级」长句。第六节「当日赛事联赛形势」重写为每联赛独立卡片：本日对阵表（场次/主客/倾向/λ总/主客近况）+ 数据看点（进球预期最高/冷门最高/最胶着）；并标注当前数据源无完整积分榜与未来赛程。近况取自 scripts/matches_data.json 的 home_recent/away_recent（引擎 MATCHES 不含该字段）。

- 第七节联赛形势接入 7M 真实积分榜：新增 _fetch_league_data.py（抓 standing.js→league_data.json，14 联赛全验证通过）与 _league_match.py（简体缩写→7M 繁体全名映射，覆盖 44/46，未匹配优雅显示「排名—」）。_gen_report.build_league_sec 重写为每联赛卡：真实积分榜（欧冠/欧联绿底、降级红底 Zone 着色）+ 本日对阵积分影响（双方当前排名/分差，≤3 分标「卡位对话」）。美职(MLS) 7M 未收录（id459 实为 USL）→ 该卡显示说明替代，未编造。qtx.com 人机验证拦截不可用。每日流水线步骤4前新增 _fetch_league_data.py。已 regenerate 报告并纪律扫描（无方法论/对话/统计量泄漏）。

- 4.1 再次改版：删除「胜率/平率/负率」三列（与「倾向」列重复）；「比分可信度」列改名「可信度」，取值改为 可信/一般/不可信（score_cred 阈值 λ≤2.6 可信、≤3.5 一般、否则不可信；同时更新逐场卡标签、🎯精选表头与说明）。已 regenerate 报告、纪律扫描通过（无方法论/对话/统计量泄漏），commit 8df6f8d 推 gh-pages（fast-forward，ls-remote 验证在线）。注：本地仓库实为 C:/Users/Jin/jinbet（含 .git，gh-pages 分支），可直推；origin/gh-pages 远程跟踪引用恒陈旧，须以 git ls-remote 为准。脚本改动(_gen_report.py 等)仍留本地未提交 master，按管线约定后续同步。

- 4.1 移除「信心」列（用户确认星级=倾向最高概率分档，与倾向列重复）。_gen_report.py 删表头<th>信心</th>与行<td>stars_html</td>，regenerate、列对齐验证通过。commit d643475 推 gh-pages（ls-remote 验证在线）。「信心星级」仍在三处：逐场深度卡、串关分组+表头、顶部摘要卡；其中串关信心池是≥3★筛选规则。已向用户征询是否一并移除（串关改倾向概率阈值分组）。

- 第七节联赛形势繁体转简体：采用「抓取保留7M原始繁体（name/note/season）+ 匹配器仍用繁体对照，覆盖率保持44/46）+ 渲染时读简体副本字段（name_zh/note_zh/season_zh）」方案。_fetch_league_data.py 加 opencc(t2s) 生成 _zh 简体字段（缺库则降级保留繁体）；_gen_report.build_league_sec 三个渲染点改读 _zh。直接转JSON为简体会让 _league_match.py 对照表（值为繁体）失配、覆盖率从44/46暴降到18/46，故不可在抓取端改原字段。已 regenerate 报告、扫描第七节无残留繁体（隆/拿为大陆通用简体写法）。commit 71b0cb6 推 gh-pages（ls-remote 验证在线）。opencc 装于受管 venv（envs/default）。

- 逐场深度卡「主队/客队排名」回填 7M 排名（用户截图指出原来显示「- / 巴甲」）：_gen_report.py 新增 league_index()/rank_of()/rank_disp() 懒加载助手（复用 _league_match 匹配器 + league_data.json），insight 卡与卡顶 team_with_rank 均接入（m[home_rank] or rank_of 回退）。结果：主队 23/24、客队 21/24 填充，未填充=美职（7M 未收录）+布鲁马波/吉维森特（不在榜），诚实显示 -。H2H场均进球仍「数据不足」（管线无 H2H 交锋库，未编造；7M f_matches.shtml 历史赛程可作后续 H2H 数据源候选）。commit b7ff898 推 gh-pages（ls-remote 验证）。

## 比分「大胆化」改造（晚 19:30，用户反馈"比分预测有些保守"）
- **量化"保守"**：众数头条平均 1.89 球 vs 实际 2.83（偏差 −0.94）；双档含≥3球档仅 9.5%（时间外 7.6%）。用户直觉正确。
- **头条不可改（硬结论）**：argmax 是单场命中最优解。4036 场/时间外 1620 场实测：
  - 众数 16.5% ＞ round(λ) 12.8%（−3.7pp）
  - **条件大胆实验（_bold_probe5.py）**：只在"众数比 λ 总量小 ≥1.5 球"的最保守子集里改头条，
    该子集内仍是 众数 14.0% ＞ 期望 9.1%（−5.0pp）。**任何分层/阈值下众数都赢，头条改不得。**
  - 分 λ 档：众数优势在 2.6~3.5 段最大（15.1% vs 10.0%），高 λ 段也不反转（全样本 λ>3.5 略负属噪声）。
- **改动**：`hit_band()` 第二档由「次高众数」→ λ 期望比分（稳档押中 + 胆档量级，互补非同质）。
  代价：双档 27.6%→26.1%（−1.5pp）；收益：含≥3球档 39%→69%，今日 8/24 场双档变化，
  双档最大档均值 2.79→3.12 球。文案"长期双档命中约 26%"恰好仍准。
- **新增 🔥 大胆档板块**（bold_sec）：按「量级档与头条总进球差」降序取前 5 场，
  给 量级档（λ期望取整）+ 极限档（同倾向再上一档，_extreme_pick 用泊松取最高格）。
  纯增量，不动头条命中率。示例：莱万特v巴萨 稳0-2 / 量级1-3 / 极限1-4。
- 提交：gh-pages 8de862a（报告）、master a8713cc（gen_report.py 同步 tools/prediction/）。
- 探针脚本：_bold_probe.py ~ _bold_probe5.py（未入库，下划线前缀被 gitignore；如需归档要改名）。

## 报告页浮动章节导航（晚 19:50，用户要求"点击标题快速跳转"）
- 实现：_gen_report.py 新增 `TOC_BLOCK` 常量（自包含 style+nav+script），插入 page 模板 body 末尾。
- **设计要点**：不硬编码章节列表 —— JS 启动后扫描 `body > .container` 内所有 h2/h3，
  按需分配 `sec-N` 锚点 id 并动态生成目录项。好处：板块增减/改名无需同步维护列表，
  历史报告也能用同一段字符串回填（幂等注入）。
- 交互：scroll spy（getBoundingClientRect().top<=140 取最后一个，触底强制选末项）高亮当前章节；
  目录超长时自动滚动保持高亮项可见；>1360px 常驻右侧 `right: max(10px,(100vw-1140px)/2)`，
  窄屏折叠为右下角圆钮 + 遮罩，点击目录项自动收起。
- 配色沿用 CSS 变量 + 兜底值 `var(--accent, #e63946)`，兼容不同报告主题。
- 验证（无浏览器环境）：node --check JS 语法 ✅；静态校验 `body > .container` 唯一、
  h2/h3 共 16 项全在作用域内、无 sec- id 冲突、div/table 开闭配平 1200/1200、97/97。
- 踩坑：TOC_BLOCK 最初放在 `</body>` 之后 → HTML 不合规（靠浏览器容错），已改到 body 内末尾。
- 提交：gh-pages 3005da7（报告）、master 7e2f4ea（tools/prediction/gen_report.py）。
- 待办（未做）：历史报告回填 —— 需则写幂等注入脚本批量处理 predictions/*/index.html。

## 导航定位修正（20:00，用户反馈"有点遮挡内容，整体往右移动一些"）
- **根因：定位公式基准算错，各宽度下均遮挡 162px**。原 `right: max(10px, (100vw-1140px)/2)` 把 1140 当容器宽，
  但容器实为 1080px；且 width 176px 未计入 ⇒ 导航左缘实际伸进内容区（1366/1440/1536/1920 全部遮挡 162px）。
- **正确约束**：容器 1080 居中 ⇒ 内容右缘距视口右 = (100vw-1080)/2；
  须满足 `right + width ≤ 该间距 - 16px`。
- **修正后**：
  ```css
  --toc-w: 140px;                       /* ≥1600px 时 168px，字号同步 0.78→0.8rem */
  right: clamp(8px, calc((100vw - 1080px)/2 - var(--toc-w) - 16px), 32px);
  ```
  clamp 上限 32px = 超宽屏时也贴视口右缘，不往内容区飘（旧公式在 1920 下 right=390px，跑到了内容中间）。
- **常驻断点 1361 → 1400px**：(100vw-1080)/2 - 140 - 16 ≥ 8 解得 100vw ≥ 1408。
  窄于此时右侧空白不足以容纳任何带文字的导航，硬放必然遮挡 ⇒ 折叠为右下角圆钮（正确取舍）。
- 验算（width 140/168）：1400→间隙12px、1440→16、1536→56、1600→60、1920→220、2560→540，全部不遮挡。
- 提交：gh-pages 95b1c89（报告）、master 4b7d1c6（tools/prediction/gen_report.py），ls-remote 双分支已核验。
- ⚠️ 环境故障：本轮 bash 的 PATH 损坏（dirname/ls/tail/git 全 not found，shim 脚本报 "cd: null directory"），
  PowerShell 工具又不回显 stdout ⇒ **改用「脚本写文件 + Read 读取」** 的方式取结果，可稳定绕过。

## 移除章节标题解释性括号（20:23，用户截图指出标题括号）
- 用户截图：🎯 今日比分精选（重点关注这几场）—— 要求类似括号内容不应存在，不用解释说明。
- 清理 7 处标题：今日比分精选、大胆档、方向串关推荐、比分串关推荐、让球盘市场偏差清单、冷门风险明细、大比分观察。
- 实现： 中 4 处硬编码 h2/h3 + 3 处  标题字符串；regenerate 报告后扫描 headings 全角括号数为 0。
- 提交：gh-pages f6fed1c｜master b5f2a90（脚本同步 tools/prediction/gen_report.py）。ls-remote 双分支一致。
- 新增纪律：报告章节标题一律不用括号解释；解释性文字放正文或说明段，不放标题。
