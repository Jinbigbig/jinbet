# 2026-09-18 工作日志

- 每日流水线全链跑通：步骤0 数据刷新（[DEDUP-ALIGN] 235/243）→ 提取 18 场（引擎 14 场）→ extras/联赛数据 → V3.4 引擎 → 报告 → 复盘 09-17 → 优化器（13 天保持默认）→ 提交推送 gh-pages `46feb0f`。
- 复盘 09-17：方向 7/11=64%、双档 9%、λ偏差 -0.30。
- DRIFT_MONITOR 报 league_baselines 漂移（德乙/韩职/荷甲），提示离线重拟合。
- 当日已按要求精简 MEMORY.md（超限截断问题解决）。

## 追加：准确率质疑专项诊断（21:00~21:30）
- 用户问「最近有预测对的么？算法是不是有问题？平均 3 场都预测不到」。
- 产出：逐日命中表（近 10 天）、1X2 校准表、模型 vs 纯市场、以及 2478 场 walk-forward 对照。
- 关键数字：近 10 天方向 92/152=60.5%（长期 51.2%）、单点 18/152=11.8%（长期 15.1%）、双档 24/152=15.8%（长期 24.0%）、比分精选一档 20/60=33.3%、大胆档 5/36=13.9%、头条 ±1 球 59.9%（长期 67.2%）。
- 归因：非模型故障。① 近 10 天是极端高比分时段（实际 3.31 球/场 vs 长期 2.86，42% 场次 4+ 球）；② 精确比分本身天花板 ~15%/单点，15 场/天期望 2.3 场；③ 1X2 三项校准偏差 ±3pp 内，方向还比长期好 9pp。
- 8 组「改良」尝试全部证伪（市场权重、平局权重、λ 缩放、象限限制、非平局优先、爆冷过滤、选取目标替换）→ 引擎/选取算法/发布页零改动，避免过拟合。
- 新增 `daily_diag.py`（可复用诊断，一条命令回答该问题）；保留 `_bt_rows.json`（2478 场回测逐场档）；生成 `diag_2026-09-18.html` 诊断报告。
- 未做：扩覆盖方案（比分精选 6→8 场 / 3→4 档）待用户选择后再改版式。

## 追加：方向（1X2）命中逐场核对（21:20~21:40）
- 用户追问「60.5% 是哪个对了？胜负方向对了？」→ 明确口径 = 胜平负三项取概率最大者，与实际比分比，不含让球。
- 逐场明细：152 场中命中 92；错 60 场里 **32 场是实际打出平局**（全窗口模型只报 3 场平、对 2 场；实际平局 34 场）→ 平局占错判 53%。
- 把握度分档（最大概率）：≥50% 90 场命中 65=72.2%；40~50% 43 场仅 39.5%（低于随机）；<40% 19 场 52.6%。剔除平局后 90/118=76.3%。
- 逐日：58/80/57/67/47/54/83/69/50/64 %（09-08→09-17）。
- 窗口内 52 场有赔率：模型 65.4% vs 纯市场去水 73.1%，两者同选 92.3% → 不高于市场。
- 产出：`diag_dir_2026-09-18.html`（逐场可核对，含错判）、`_dir_detail.py`、`_gen_dir_page.py`、`_dir_rows.json`。未改引擎/选取算法/发布页。

## 追加：定位「方向」在报告中的体现位置（21:24）
- 用户追问「胜平负方向在哪预测的？在哪体现的？」→ 报告无独立「方向」栏，正式名 = **倾向**，共 5 处：第三节逐场卡片、第四节 4.1 表第 4 列「倾向」、核心策略星级、方向串关、第二节三率分布图。
- 澄清易误读点：4.1 表里 `主胜 43.8%` 的百分比 = 模型给该方向的概率（把握度），非命中率。
- 报告自带的星级长期方向命中：1★37.5 / 2★49.5 / 3★50.4 / 4★60.4 / 5★74.4% —— 与本轮把握度分档结论一致（<50% 不可信、≥50% 可用）。
- 今日 09-18 共 14 场倾向中仅 5 场把握度 ≥50%（006 格罗宁根 61.2%、007 登博思 60.6%、008 拜仁 86.4%、013 西班牙人 54.8%、014 纽约城 50.5%）。
- 线上地址确认可访问：`https://jinbigbig.github.io/jinbet/` 与 `.../predictions/2026-09-18/` 均 200。

## 追加：4.1 表「可信度」改为「方向可信度」（21:27~21:45，已上线）
- 改动：`_gen_report.py` 新增 `dir_cred(m)`（方向确定性分档：≥50% 可用 / 40%~50% 慎用 / <40% 别跟），**仅用于 4.1 表**；列名「可信度」→「方向可信度」，说明文字同步。
- 保留 `score_cred`（λ 分档的比分口径）用于**逐场卡片**与**今日比分精选榜** —— 两套口径分工：dir_cred=方向能不能跟，score_cred=比分能不能照抄。
- 决策点：冷启动场次**不再显示「数据不足」**，照常按方向概率分档（方向概率来自赔率反推，纯市场口径，判读有效；数据薄只影响比分侧）。
- 今日 14 场结果：可用 5（006 格罗宁根 61.2/007 登博思 60.6/008 拜仁 86.4/013 西班牙人 54.8/014 纽约城 50.5）、慎用 6、别跟 3。
- **推送插曲（重要教训）**：gh-pages 远端有外部追加提交（`46feb0f` 之后 `19d19c5`/`6682b26` 两次 data 提交），且这两个提交把 `odds_history/2026-09-10.json`、`results_history/2026-09-11.json` 纳入跟踪，而本地这两文件是 untracked → `git rebase` 直接被 "untracked working tree files would be overwritten" 卡死；随后 `rebase --abort` 触发 `reset --hard` 把这两个 untracked 文件删掉，并让 `.workbuddy/memory/2026-09-{13,16,17}.md` 显示为已删除。
- **结论口径**：此种情形**不要再试 rebase**，直接走记忆里那条 `cp 报告 → git reset --hard <ls-remote 真实SHA> → 写回报告 → 单提交 → push`；本次即以此成功（`6682b26 → e1bd232 → d9ce365`）。
- 数据文件最终取**云端版本**（条数一致、内容为最新赔率刷新），本地原版已弃。

## 追加：三引擎解耦重构（21:45~22:00，已上线）
**用户需求**：① 重做一套按双方进球联合分布预测比分的引擎（可用泊松/双变量泊松/蒙特卡洛/xG，必须估出两队各进几球）；② 现有引擎继续算胜平负，4.1 拆分为「胜负」单表 + 新增「让球」列，去掉命中比分/双档；③ 最终三套引擎、互不影响。

**落地**
- 新建 `score_engine.py`（引擎 B，~610 行，自带 `Engine` 类：load_history/feed/rating_lambdas/market_lambdas/joint/blend_market/monte_carlo/predict）。双变量泊松（Karlis–Ntzoufras λ3 共同分量）+ 主客分拆攻防评级（半衰期 120 天、向 1.0 收缩 k=4、Gauss-Seidel 对手强度）+ 市场 1X2 去水一维反解分配 λ + 竞彩比分盘 70% 融合 + 2 万次蒙特卡洛校验。
- 引擎 A 仍 = `_calc_engine.py`（未动）；引擎 C 仍 = `selection_algo.py`（未动）。
- `_gen_report.py`：新增 `score_pred/head_score/rq_cells/lam_cred`；4.1 改「编号/主队/客队/倾向/方向可信度/让球盘/让球判读/冷门」；新增 4.2 比分预测表（预期进球、双方各自最可能进球数、最可能比分、±1球/Top3 覆盖、可信度）；原 4.2~4.5 顺延为 4.3~4.6。逐场卡片比分区改用引擎 B（带回退 `hit_pick`），比分精选榜/大胆档头条仍用引擎 C 的 `head_score`。
- 参数（2478 场 walk-forward 定）：`half_life=120, k_shrink=4, opp_adj=1, score_w=0.50, lam3=0.08, tau 关, market_mix=0.70, max_goal=8, mc_n=20000, league_eb_k=40`。
- 实测：引擎 B Top1 13.88% / Top3 34.26% / ±1球 66.18% / 1X2 51.61%；lam3 与 tau 扫描均落在噪声内或轻微负向 → 取小值/关闭。蒙特卡洛 vs 解析网格：头条概率最大偏差 0.80pp、均值 0.33pp。
- **互不影响验证**：改 B 参数（lam3=0.30、market_mix=0）→ 4.1 完全不变、4.2 变；改 C 参数（bd_total_shift=2、pk_degen_down=0.5）→ 4.1、4.2 均不变。
- **顺带修的坑**：① `results_history/` 235 个文件主客整体反序（官方锚交叉核对：1032/1065 顺序颠倒）→ `dedup_results_history.py --apply --no-net` 修复，4635→3104，主胜 42.6%/平 26.3%/客胜 31.2%；② `market_dist` 误含「胜其他/平其他/负其他」致 `int()` 崩，改为只取 0:0~5:5；③ `rq_cells` 无让球盘分支返回长度不齐致解包崩，改规范 2 元组。
- **推送事故与处置**：master 远端 force-update 后 `git worktree add` 用旧 SHA 失败，误把 `_wt_master/tools/prediction/` 提交进 gh-pages（4ce4625）→ `git reset --hard 562624d` 撤销，删 `_wt_master`，重新 fetch + 用 FETCH_HEAD 建 worktree，正确落到 master。
- **落盘**：gh-pages `66ed89f → e51ac27 → 562624d`；master `ed5735f`。线上 `predictions/2026-09-18/` 200（264KB），含「4.1 胜负预测 / 4.2 比分预测 / 让球判读 / 最可能进球(主)」；红线 grep 为空。
- 离线资产（未提交，工作盘）：`_fit_score_engine.py`、`_bt_rows2.json`。

**注意**
- 公开页「🎯 今日比分精选」表里仍保留「命中比分/双档」列 —— 那是引擎 C 的比分榜，用户只要求从 **4.1** 去掉，勿误删。
- 用户明确表示**不用反复提示比分准确率天花板**，已知悉，勿再重复提醒。

## 追加：用户反馈「003/004/012 有让球盘却说没有 + 引擎B的比分没变化」→ 定位并修复上游别名行（22:00~22:20，已上线）
**症状**：① 4.1 表对 周五001/003/004/012 显示「无让球盘」（实际竞彩都开了让球）；② 用户感觉 4.2 比分与老引擎一致、「一定不对」。

**根因（一个 bug 同时造成两个症状）**：`scripts/matches_data.json` 有 **18 行而真实只有 14 场** —— 上游 SCHEDULE 同场生成了两行：
- 「官方简称行」：沃夫斯堡/布城/奥斯KFUM/卡塔尔23，带真实赔率、让球 1 档、比分盘 31 档、近期战绩、matchId 5498xxx；
- 「长名行」：沃尔夫斯堡/布里斯托尔城/奥斯陆KFUM/卡塔尔亚足，**赔率全空、让球/比分盘为空、战绩全空**、matchId 2041xxx（赛果 API 旧 ID）。

`_calc_engine.py` 用 `{m["matchNumStr"]: m}` 建表 → **后写胜**，恰好保留空行 → 这 4 场同时丢掉让球盘（为什么 4.1 显示「无让球盘」）+ 比分盘 + 球队评级历史（为什么引擎 B 只有 1:1、看不出新引擎的影响）。核对：`results_history` 里 沃夫斯堡 20 场 / 布城 6 / 奥斯KFUM 11 / 卡塔尔23 2，长名写法 **0 场** → 长名行是污染，官方简称才是 canonical。

**处置**
1. 删 `matches_data.json` 4 行空别名行（18→14，保留带数据的那行）；013 的 home_rank/away_rank 等 extras 均保留。
2. `update_odds_net.RESULT_TEAM_NAME_MAP` 补 5 条映射（沃尔夫斯堡→沃夫斯堡、达姆施塔特→达姆施塔、布里斯托尔城→布城、奥斯陆KFUM→奥斯KFUM、卡塔尔亚足→卡塔尔23），从源头不再生成别名行。
3. `_calc_engine.py` 新增 `_odds_richness()` + 同编号择行（1X2>让球盘>比分盘>战绩条数），并在日志打「[场次去重] 同编号多行 N 处」。
4. `_local_prepare.py`（工作盘脚本）：SCHEDULE 按 matchNumStr 去重（优先带 matchId 的行）+ ODDS 键容错匹配（同日 + 队名粗归一包含判断），未命中会显式告警。
5. 重跑引擎 A（14 场全部拿到让球盘）→ 重跑报告。

**数字变化（修复前 → 修复后）**
- 001 沙特阿拉伯/卡塔尔23：倾向 主胜 43.8% → **64.7%（可用）**；比分 1:1 → **1:0**；λ 1.57/1.29 → 1.93/0.86。
- 003 沃夫斯堡/达姆施塔：42.8% → **47.9%**；1:1 → **2:1**；λ 1.64/1.37 → 2.02/0.89；让球负 EV 1.13。
- 004 萨尔普斯堡/奥斯KFUM：46.7% → **65.6%（可用）**；1:1 → **2:1**；λ 1.63/1.42 → 1.95/0.84。
- 012 布城/沃特福德：39.7% → **51.7%（可用）**；1:0 → **1:1**；λ 1.55/0.94 → 1.72/1.03。
- 引擎 B 与市场比分盘头条：**12/14 一致**（修复前那 4 场无输入必然发散）；n_hist 从 0 恢复（003 主 9/客 5、004 3/5、012 4/5）。

**引擎 B 配置复核（回应用户「比分一定不对」）**
- 1248 场（含比分盘）对照：生产（现行 λ + 70% 市场融合）Top1 **16.43%**/Top3 37.98%/±1球 68.91%，优于「改用市场隐含 λ」14.90% 与「市场权重 100%」14.90% → 现行参数已是最优，不调。
- λ 总量偏差：引擎 λ总 / 市场隐含 λ总 **中位 0.981**、均值 0.964（P10 0.649 / P90 1.247）→ 无系统性高估/低估。
- 头条 9/14 与老引擎相同的原因：这些场 λ 都低（2.2~3.4），1:1 本就是联合分布众数；但概率与分布已不同（如 001 老 14.5% → 新 11.7%），且引擎 B 额外给出双方各自进球数与 ±1球/Top3 覆盖。

**落盘**：gh-pages `fc155d5`；master `9711c15`（`tools/prediction/calc_engine.py` + 根 `update_odds_net.py`）。线上 4.1 已复核：14 场全部有让球盘。备份在 `_recover_20260918/`，日志 `_recalc_2026-09-18.log` / `_regent_2026-09-18.log`。


