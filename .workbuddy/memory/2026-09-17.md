# 2026-09-17 工作日志

## 一、JinBet 每日流水线（自动化 b0bc3264）
- 全链跑通：步骤0（赔率+赛果）→1 `_local_prepare` →2 `_build_today_extras` →2.5 `_fetch_league_data` →3 `_calc_engine` →4 `_gen_report` →4.5 `gen_review --date 2026-09-16` →4.6 `_optimize_selection`。
- 当日 11 场（欧罗巴 x6／西甲 x2／亚运女足 x1／亚冠乙 x1／解放者杯 x1），冷启动 2 场 → 非冷启动 9 场 → 出 1 档（比分精选 6 / 大胆档 4）。
- 步骤0b 出现 `[DEDUP-ALIGN]`：受影响 235/242 文件（归档污染照例复发并自愈），备份 `_hist_backup_2026-09-17`。
- 引擎链路校验：chain 含「市场混合(…总量守恒)」+「比分矩阵对齐发布1X2」，路线图 ON·生效/ON·观察 齐全，Platt 已拟合（n=2010），11/11 算完。
- 复盘 2026-09-16：方向 8/16=50%、比分单点 2/16=12%、双档 1/16=6%、λ总进球偏差 -0.07 球/场。
- 推送：gh-pages `44b75c2 → 4844b06`；复盘页/报告页线上 200。

## 二、比分精选/大胆档 选取口径修复（用户反馈「命中率太差」后排查）
**查到的两个真问题**
1. `_optimize_selection.py` 回放**自写排序**且**不排除冷启动**，与生产的 `SA.rank_pk/SA.rank_bold`（排冷启动）不是同一算法 → 4/12 天选出的场次不同，过往「保持默认」的结论不可信。
2. 优化器 `bd_lambda_min` 网格只到 2.0，而真实改善区在 2.4~3.2 → 每天必然报「无改善」。

**12 天回放真实命中率（生产口径，第一档）**
- 比分精选 23/72 = 31.9%（含双档；单点 ~22%）— 明显高于「双档约 20.5%」基准，**无需改动**（cap3=3.4/degen=0.85 已是网格最优 55.83）。
- 大胆档 5/48 = 10.4%，命中日 3/12 — 偏弱。
- 大胆档病灶：全历史 200 场非冷启动中 **61.0% 的量级档 == 头条**，排序键 `max(极限档,量级档)` 因此系统性偏向低 λ、低比分场次，与「大胆/进攻」定位相反。

**改动**
- `selection_algo.bd_tuple`：`bd_lambda_min` 由「硬剔除」改为「λ 达标优先占位」（未达标者质量分 −1000 整体让位），低进球日档位不再塌缩；`bold_quality` 回归纯概率口径。**返回值保持 2 元组**（报告侧按 `(-x[0], -x[1])` 与 `x[2]` 解包，改 3 元组会连环崩——已踩过）。
- `_optimize_selection.py`：回放改调 `SA.rank_pk`/`SA.rank_bold`（唯一事实源、排冷启动），并放宽网格（cap 3.0~4.2、λ门槛 0~3.2）。
- 结果：**采纳 `bd_lambda_min=3.0`**（目标 16.25→28.75，命中率 10.4%→14.6%，命中日 3/12→6/12；2.8~3.0 平台、3.2 起回落）。比分精选保持默认。

**今日大胆档变化**：`水晶宫vs波兹南`、`乌兹别女vs中国女`（高 λ）上位，补位 `利勒斯特`、`尤文图斯`，挤掉 `贝蒂斯`、`贝西克塔`。

**落盘**：gh-pages `4844b06`；master `tools/prediction/selection_algo.py` 正本 `e2ce110`（worktree 同步后移除）。线上已核验。

## 三、踩坑记录（重要）
- ❗`git rebase` 在远端被 force-update 后，默认 `--fork-point` 会从 reflog 取陈旧基点 → 本次把 `b119ac2` 当基点，误重放 **247 个提交**并卡冲突。改用 `git rebase --no-fork-point <ls-remote 真实SHA>` 一次通过。
- ❗remote-tracking ref（`origin/gh-pages`）被并发会话覆写成陈旧 `b119ac2`；**判断基点一律用 `git ls-remote` 的 SHA**。推送用 `git push origin HEAD:refs/heads/gh-pages`。
- worktree 在并发下 admin 目录会消失（`_wt_master` 曾失效）→ 先 `git worktree prune`，用 `git worktree add --detach <path> <真实SHA>`。
- 期间 `.workbuddy/memory/2026-09-17.md` 被 rebase 折腾丢失，已重建。

## 五、快照冷启动字段缺失（第二次排查，用户追问「修复的是这两档么」）
- 追问后复核「改动范围」：**代码层面两档共用一份实现都被审计，但实际生效只动了大胆档**。实测今日两档在默认参数 vs 新参数下的名单：比分精选 6 场**完全一致**（`pk_lambda_caps`/`pk_degen_down` 本就等于默认），大胆档换 2 席。
- ❗**新发现的第三个 bug（根因）**：`calc_engine.dump_prediction_snapshot` **不写 `cold`/`data_n`** → 快照里这两字段恒缺失 → `SA.is_cold()` 对**全部历史场次恒为 False** → 任何基于快照的回放/回顾都把冷启动场次当正常场次参与排序（优化器、报告「前日回顾」都中招）。报告正文不受影响（它读 `_calc_result.json`，里面有 cold）。
- 修复：① `_calc_engine.py` 快照 rows 增写 `"cold"`/`"data_n"`；② 用 `_calc_result.json` 回填 `predictions/2026-09-17/pred_snapshot.json`（2 场冷启动：周四002 上海申花、周四003 克里特）；③ master 正本 `tools/prediction/calc_engine.py` 同步该补丁 **+ 补上 09-16 的动态市场权重修复**（master 正本此前仍是固定 `MARKET_W=0.80`，与实跑 `league_market_w()` 脱节）。
- 校验：回填后用回放口径 `SA.rank_pk` 重算今日比分精选 = `009/011/004/007/006/010`，与报告实际发布**逐条一致**，证明口径已对齐。
- 落盘：gh-pages `5cbf56e → 5992d3c`（仅快照回填）；master `e2ce110 → e0c200c`（worktree 同步后已移除）。线上两页仍 200。
- ⚠️ 历史日 `pred_snapshot.json`（09-16 及更早）**均无 cold/data_n，无法回溯**（根目录 `_calc_result.json` 每日覆盖）→ 过往 12 天回放仍是「冷启动盲」的口径，`bd_lambda_min=3.0` 即在此口径下选出，后续需观察是否需要按新口径复评。
- ✔️ 另确认：报告侧 `_conf_rank`/`_bold_rank` 只是薄封装，**实际调用 `SA.pk_tuple`/`SA.bd_tuple`**，没有第二份排序实现（此前担心的重复实现问题不存在）。

## 六、用户澄清「两档要单独计算」（重要，待落地）
- 用户原话要点：**两档要单独计算，不和任何相关联**；「整体还是用原来的方式去计算」（引擎口径不动）；目的=**每榜数据量小，最大化优化小样本以提高准确率**。
- ❗我昨天的理解偏了：只做了「同池内换排序键」→ 用户明确指出「不是像你刚刚换两个的形式」。
- 已量化的三处残余耦合：① 候选池共用（可重叠，近 12 天大胆档 22.9% 的腿与比分精选同场）；② 大胆档借用比分精选的 λ 质量概念（我昨天加的 `bd_lambda_min`）；③ 两榜共用引擎同一份比分矩阵。
- 预演「互斥分池」效果（12 天回放）：7/44=15.9% vs 现状 7/48=14.6%，**命中日不变（6/12）**，但低场次日档位缩水（09-10 只剩 1 腿）→ 单靠互斥收益很小，需与「独立调参/独立建模」配套。
- 已向用户发问确认解耦范围（互斥分池 / 独立调参 / 独立建模 / 三者全做），**等答复后再动 selection_algo**。

## 四、待办 / 观察项
- DRIFT_MONITOR 报 `league_baselines` 漂移（均值变化率 3.61%、标准差 7.19%；德乙 +50%），提示可离线重拟合 market_calib / Platt；近 3 日方向命中率未持续 <50%，本轮未触发。
- ⚠️ 报告「前日回顾」用**当前**调参重算（非当日实发口径）：改参数后 09-16 大胆档回顾由 0/4 变成 1/4，而当日实际发布是 0/4。若要让回顾忠实反映当日发布，需把当日 tuning 存进 `pred_snapshot.json` 再回读——待用户决定是否做。
