
## 14:00 常规流水线（自动化 b0bc3264）
- 全链跑通：30 场（赔率 30/30），gh-pages `8e17078 → 13d93da → 9e3d1ca`（ls-remote 核验），线上报告/复盘/tuning 均 200。
- 步骤0 `[DEDUP-ALIGN]` 照例生效（235/244）；别名行防护正常（matches_data 无重复编号）。
- 复盘 09-18（14 场）：方向 7/14=50%、单点 14%、双档 29%、λ偏差 -0.09（阈值内，不触发重拟合）。
- 4.6 优化：14 天，比分精选保持默认；**大胆档采纳 bd_min_total 2→3**（30.71→32.50，holdout 13.75→27.50），已重跑报告+提交。
- DRIFT 连续第二日报 league_baselines 漂移（德乙 50%/韩职 16.7%/荷甲 15.4%）→ 已在回复提示离线重拟合 market_calib/Platt。
- 事故+修复：rebase 时 `--no-fork-point <SHA> origin/gh-pages` 带第二参数 → detached 到 origin 空操作；本地存在外部作业（Trae Bot 13:52）未推送提交 a0bbeab 导致 rebase 冲突 → 改用 reset --hard 8e17078 + checkout e227f5d -- . 单提交推；但 `git add -A` 把 2147 个工作盘临时文件误入裤，已二次提交 `git rm -r --cached` 清理（9e3d1ca）。教训：提交永远 `git add -u` + 显式 add。
- 已按要求精简 MEMORY.md（原超限被截断注入）。

## 20:30 头条比分口径改造（用户："我不要这种众数分布"）
- 用户指出 09-19 报告 001–016 连续头条 1:1（全卡 26/30）。诊断：众数口径下两队 λ 同落 [1,2) 必然 1:1（长期占预测 56%），非故障但连片、区分度差。
- 实测（2478 场 walk-forward，新增 `headline_rule_probe.py`）：众数 15.13%/1:1占56% → gap5 15.54%/23% → gap7 14.57% → gap10 13.44%；「象限内+gap5」**15.58%**（分半 14.29%/16.87% 两半均不劣于基线）、1:1 **24%**、且倾向=平局时保持 1:1、绝不与发布倾向矛盾 → 采纳。
- 实现（唯一实现 = `score_engine.headline_reorder(top, gap=5.0, dir_key)`，DEFAULT 新增 `head_gap=5.0`）：直接重排 top_scores 的位次，故报告列 / SA.hit_pick / SA.band_scores 自动跟随，**无第二份口径代码**。`_calc_engine._headline_rank()` 以引擎 A 倾向为 dir_key 调用它（engine A 快照的 top_scores 原本自己重建、会绕开引擎 B 的规则）。
- 今日效果：1:1 26/30 → 5/30（2:1×10、1:2×9、1:1×5、1:0×4、0:1、2:0）；与倾向象限矛盾 4 场（皆为保留 1:1 的场合）。
- 落盘：gh-pages `d12fe5b → fba2ce1`、master `7f1d7d0 → f3f7c96 → b58204b`（含探针）；线上报告 200 已复核。

## 21:00 修复「可能比分1/2 颠倒」（用户第二次反馈）
- 用户指出 4.2 表「可能比分1」概率**低于**「可能比分2」（001 场 1:2=11.7% 在左、1:1=12.0% 在右）。根因=20:30 那次改造**直接重排了 top_scores 位次**，把概率事实列污染成「首选在前」。
- 先量化再动手（`band_rule_probe.py`，2478 场）：象限内排序 首选14.21%/双档15.78%/前三29.98% vs 纯概率 15.13/24.01/39.14 → **象限口径不可推广到比分列/双档**（-5~9pp）。
- 定稿口径：`top_scores` **恒为纯概率降序**；首选另立 `headline` 字段（快照）/`SA.headline_pick()` 现算（报告）。双档 = **除头条外概率最高的两个**（实测 23.57%，含头条口径的前二并集不变）。
- 报告改动：4.2 列 `可能比分1/可能比分2` → **`比分预测`（首选，accent）+ `比分双档`（两个，各带概率）**；卡片「命中比分/可能比分1」文案同步；`gen_review.predict_scores` 头条优先读 `headline`，老快照回退众数（历史复盘口径不变）。
- 比分精选榜：`pk_ref` 头条改走同一份 `headline_pick`（boards 也套用），`pk_head_probe.py` 回放 225 场显示 **头条 11.11%→8.89%、双档 17.33%→19.56%、合计不变 28.44%**、1:1 占比 63%→14%。今日 1:1 分布：4.2 首选 1/30（原 26/30）、精选榜 6/18（原 18/18）、大胆档 0/4。
- 口径变了故重跑 4.6：`selection_tuning.json` 采纳 pk（旧 1:1 降权 `pk_degen_down` 0.85→1.0、`pk_lambda_caps` 3.4→3.0）与 bd（`bd_total_shift` -1→0）；已重跑报告。
- 落盘：gh-pages `6883217→d38d942`，master `b58204b→43be6da`（含 3 个探针）。⚠️ 首次 curl 拿到的是 CDN 旧缓存（另一作业的 V3.3 版报告），带 `?v=` 破缓存后确认线上已是新版。
